name: ci

on: [push]

jobs:
  test:
    runs-on: macos-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Build
        run: swift build

      - name: Test
        run: swift test
